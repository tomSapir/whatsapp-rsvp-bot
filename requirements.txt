# WhatsApp RSVP Bot — Python dependencies (see STEPS.md M0)
#
# Floors are declared here; run `pip freeze > requirements.lock` after the first
# install if you want fully reproducible, exact-version pins.

# --- Web engine ---
fastapi>=0.115
uvicorn[standard]>=0.34

# --- Data layer ---
SQLAlchemy>=2.0
pydantic-settings>=2.7

# --- Domain helpers ---
phonenumbers>=8.13

# --- External clients ---
openai>=1.60
httpx>=0.28

# --- Reminders ---
# Pin to 3.x: APScheduler 4 changes the scheduler API the reminder job uses.
APScheduler>=3.10,<4

# --- Host UI ---
streamlit>=1.41

# --- Tests ---
pytest>=8.3
